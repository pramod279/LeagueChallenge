/**
 * This Gradle Contains App Version Related Information
 */
def app_info = [:]

/**
 * Application ID
 */
app_info.applicationID = "life.league.challenge.java"

/**
 * Application Name
 */
app_info.applicationName = "League"

/**
 * Build Version Information
 */
app_info.version_code = 1
app_info.version_name = "1.0"

ext.app_info = app_info

/**
 * Build Version Information
 */
def build_version = [:]

/**
 * Minimum & Maximum Supported Versions for the App
 */
build_version.min_sdk = 21
build_version.target_sdk = 31
build_version.build_tools_version = "30.0.3"

ext.build_version = build_version